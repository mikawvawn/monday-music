{
  "name": "monday-music",
  "version": "1.0.0",
  "description": "Weekly music discovery playlist generator",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "test": "tsc && node --test dist/*.test.js",
    "setup:env": "bash scripts/setup-env.sh"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}
